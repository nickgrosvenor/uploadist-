<%= form_for(@album_photo) do |f| %>
  <% if @album_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album_photo.errors.count, "error") %> prohibited this album_photo from being saved:</h2>

      <ul>
      <% @album_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %><br>
    <%= f.text_field :photo_id %>
  </div>
  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.text_field :album_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
